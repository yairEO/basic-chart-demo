1. Make sure `pnpm` is [globally installed](https://pnpm.io/installation)
2. Run `pnpm i` command to install project's packages (in the root folder)
3. Run `pnpm dev` to start the dev server (should auto-open a browser)